import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['img.otruyenapi.com', 'mangadex.org','storage-ct.lrclib.net','s4.anilist.co','img.htrcdn.com'],
  },
};

export default nextConfig;
